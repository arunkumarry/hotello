<!-- <p id="notice"><%= notice %></p> -->

<div class="jumbotron">
  <h2 class="text-center">Hotello is the best site for reviews & ratings of the hotels considering various parameters!!!</h2>
  <h3 class="text-center">Explore all type of food zones in your community<br>
    Leave ratings and reviews help your fellow foodies.
  </h3>
</div>
<!-- <h1 class="text-center">Listing Restaurants</h1> -->
<div class="well">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Name</th>
        <th>Address</th>
        <th>Phone</th>
        <th>Website</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @restaurants.each do |restaurant| %>
        <tr>
          <td><%= restaurant.name %></td>
          <td><%= restaurant.address %></td>
          <td><%= restaurant.phone %></td>
          <td><%= restaurant.website %></td>
          <td><%= link_to 'Show', restaurant, class: "btn btn-success"%></td>
          <td><%= link_to 'Edit', edit_restaurant_path(restaurant), class: "btn btn-primary" %></td>
          <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
</div>
<%= link_to 'New Restaurant', new_restaurant_path, class: "btn btn-success" %>

<div class="bottom-image">
  <%= image_tag "http://clipart-finder.com/data/mini/1290032361.png" %>
</div>